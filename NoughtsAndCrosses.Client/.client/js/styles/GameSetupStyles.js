const styles = theme => ({
    root: {
        display: 'flex',
        flexWrap: 'wrap',
    },
    card: {
        minWidth: 100,
    },
    formControl: {
        margin: theme.spacing.unit * 3,
    },
    group: {
        margin: `${theme.spacing.unit}px 0`,
    },
});
export default styles;